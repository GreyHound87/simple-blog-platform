const usernamePatternRule = {
  pattern: /^[a-z][a-z0-9]*$/,
  message: 'Should start with a lowercase letter and can only contain lowercase letters and numbers',
}

export default usernamePatternRule
